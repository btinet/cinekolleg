{% extends 'base.html.twig' %}

{% block title %}Profilübersicht{% endblock %}

{% block body %}
    {{ include('component/_navbar.html.twig') }}
    <section class="py-5" id="overview">
        <div class="container py-5">
            <div class="section section-primary">
                <p class="section-lead">{{ app.user.firstName }}</p>
                <h1 class="section-title mb-3">Profil</h1>
            </div>
            {% if not user.verified %}
            <div class="row border-bottom pb-3 g-3 align-items-center justify-content-between">
                <div class="col-12 col-md-8 text-center text-md-start">
                    <span>Deine E-Mail-Adresse ist noch nicht bestätigt</span>
                </div>
                <div class="col-12 col-md-4 text-center text-md-end">
                    <a href="{{ path('profile_verify_email') }}" class="btn btn-secondary">E-Mail verifizieren</a>
                </div>
            </div>
            {% endif %}
            <h4 class="section-lead">
                Kontodaten
            </h4>
            {{ form_start(user_form) }}
            {{ form_end(user_form) }}
            <h4 class="section-lead">
                E-Mail-Benachrichtigung
            </h4>
            {{ form_start(notification_form) }}
            {% block _notification_sources_row %}
            {% endblock %}
            {{ form_end(notification_form) }}
        </div>
    </section>
{% endblock %}
